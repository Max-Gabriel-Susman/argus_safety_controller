name: ci

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup ROS 2
        uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: humble

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-rosdep python3-vcstool python3-colcon-common-extensions \
            build-essential cmake git rsync

          sudo rosdep init 2>/dev/null || true
          rosdep update

      - name: Create workspace and pull deps
        shell: bash
        run: |
          set -euo pipefail
          set +u
          source /opt/ros/humble/setup.bash
          set -u

          mkdir -p ws/src
          rsync -a --exclude ws ./ ws/src/argus_safety_controller

          cd ws/src

          git clone -b humble https://github.com/ros2/rclc.git
          git clone -b humble https://github.com/micro-ROS/rmw_microxrcedds.git
          git clone -b humble https://github.com/micro-ROS/micro_ros_msgs.git
          git clone -b humble https://github.com/micro-ROS/micro_ros_utilities.git
          git clone -b humble https://github.com/micro-ROS/rosidl_typesupport_microxrcedds.git

          git clone -b v2.0.1 https://github.com/eProsima/Micro-CDR.git microcdr
          git clone -b v2.4.3 https://github.com/eProsima/Micro-XRCE-DDS-Client.git microxrcedds_client

      - name: rosdep install
        shell: bash
        run: |
          set -euo pipefail
          set +u
          source /opt/ros/humble/setup.bash
          set -u

          cd ws
          rosdep install --from-paths src --ignore-src -r -y \
            --skip-keys "rosidl_typesupport_microxrcedds_c rosidl_typesupport_microxrcedds_cpp"

      # - name: Build
      #   shell: bash
      #   run: |
      #     set -euo pipefail
      #     set +u
      #     source /opt/ros/humble/setup.bash
      #     set -u

      #     cd ws

      #     colcon build \
      #       --merge-install \
      #       --symlink-install \
      #       --event-handlers console_direct+ \
      #       --executor sequential \
      #       --parallel-workers 1 \
      #       --cmake-args \
      #         -DBUILD_TESTING=OFF \
      #         -DUCDR_BUILD_TESTS=OFF \
      #         -DUCLIENT_BUILD_TESTS=OFF

      # - name: Dump colcon logs on failure
      #   if: failure()
      #   shell: bash
      #   run: |
      #     set -euo pipefail
      #     echo "==== colcon log tree ===="
      #     find ws/log -maxdepth 4 -type f | sort || true

      #     echo "==== last 300 lines of colcon build logs (if present) ===="
      #     # Try a few common log locations
      #     for f in \
      #       ws/log/latest_build/*/stderr.log \
      #       ws/log/latest_build/*/stdout.log \
      #       ws/log/latest/*/stderr.log \
      #       ws/log/latest/*/stdout.log
      #     do
      #       if [ -f "$f" ]; then
      #         echo "---- $f ----"
      #         tail -n 300 "$f"
      #       fi
      #     done

      # - name: Upload colcon logs
      #   if: failure()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: colcon-logs
      #     path: ws/log

      # - name: Test
      #   shell: bash
      #   run: |
      #     set -euo pipefail
      #     set +u
      #     source /opt/ros/humble/setup.bash
      #     source ws/install/setup.bash
      #     set -u

      #     cd ws
      #     colcon test --event-handlers console_direct+ --packages-select argus_safety_controller
      #     colcon test-result --verbose