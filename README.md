# Argus Safety Controller

The **Argus Safety Controller** targets Cortex A9 MCU on the Arty Z7. This firmware application is intended to let the embedded device publish telemetry into the Argus ROS 2 graph. Eventually efforts will be put towards safety once we're receiving inbound commands for stimulus from the host side. The short-term plan is to:

- [ ] 1. Migrate the neural decoding logic from the argus neural interface firmware project to the gateware in the argus-neural-codec repo and provide safe gateware access to the Argus Cybernetics Stack's ROS graph. This will target the Arty Z7'sPL.

- [ ] 2. Port the FreeRTOS implementation over to baremetal.

- [ ] 3. Modify the implementation to be closed loop.

The long-term plan is for the safety controller to be responsible for coordinating system-level constraints, validating operating modes, enforcing safety boundaries, and determining when FPGA-based processing or control paths are allowed to participate in the larger closed-loop system.

## Build System

This project currently uses CMake as its build system for the embedded software application. Source files for the embedded application are registered through the Cmake configuration in `UserConfig.cmake`. You'll need to update the `sources` section in `UserConfig.cmake to include new source files so that Vitis includes them in the build.

## Vendored dependencies

Two dependencies are vendored into `safety_controller/src/` rather than
consumed as packages. Both are committed; neither is generated at build time.

### lwIP + Xilinx GEM port

Vitis 2026.1's BSP indexer does not register `lwip220` as a library for
`ps7_cortexa9`, despite `lwip220_v1_4` shipping a complete descriptor set
listing that processor. The library never appears in `.repo.yaml`, so it
cannot be added to the domain. We compile the sources directly instead.

To re-vendor (after a toolchain reinstall or version bump):

    LW=/tools/Xilinx/2026.1/Vitis/data/embeddedsw/ThirdParty/sw_services/lwip220_v1_4/src/lwip-2.2.0 \
      ./tools/vendor_lwip.sh

The script defaults to the Linux install path; override `LW` for other
locations. It copies 26 `.c` files into `safety_controller/src/lwip/`.
Check available versions with:

    ls -d /tools/Xilinx/*/Vitis/data/embeddedsw/ThirdParty/sw_services/lwip220_v*/src/lwip-2.2.0

### argus_wire.h

Canonical definition lives in `argus_core/include/argus_core/argus_wire.h`.
In a properly setup(TODO: document this process) workspace re-vendor with:
```
cp ~/Documents/argus_ws/src/argus_core/include/argus_core/argus_wire.h ~/Documents/argus_safety_controller/safety_controller/src/
```
Then restore the provenance comment at the top of the copy. Any change to
this header must land in `argus_core` first, and `argus_sensors` on the Orin
must be updated in the same change — all three parse the same frames.

## Hand-maintained BSP headers

The BSP normally generates `lwipopts.h` and `xlwipconfig.h` from the
`lwip220` library settings. Because the library isn't registered, these are
maintained by hand in `safety_controller/src/` and committed.

`xlwipconfig.h` gotcha: the lwIP port tests controller macros with `#ifdef`,
not `#if`. Defining `XLWIP_CONFIG_INCLUDE_EMACLITE 0` enables EmacLite. Only
`XLWIP_CONFIG_INCLUDE_GEM` should be present; the rest must be left undefined.

## Build configuration

`safety_controller/src/CMakeLists.txt` is generated by Vitis and will be
overwritten. All customization belongs in `UserConfig.cmake` via
`USER_INCLUDE_DIRECTORIES` and `USER_COMPILE_SOURCES`. Note that the stock
file sets both variables twice — the later `set()` wins, so edits must go in
the canonical (later) location, not a block inserted above it.

After changing vendored sources or `UserConfig.cmake`, wipe the build
directory so CMake re-runs its `file(GLOB)`:

    rm -rf safety_controller/build

## Documentation

needs impl.

### Doxygen

needs impl.

## Glossary

Xilinx Support Archive(XSA): A *.xsa file is a Vivado generated handoff file that tells Vitis what hardware exists on the FPGA-SoC design for building a matching embedded software platform.
